model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String?
  socialId  String?
  provider  String?
  nome      String?
  createdAt DateTime @default(now())

  @@map("users")
}
